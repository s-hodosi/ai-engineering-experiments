## Purpose

~~Adzuna API-based job search across 7 EU countries, providing 1–3 hour freshness as a parallel source alongside Tavily.~~

**Retired**: Adzuna source replaced by LinkedIn email parsing. Adzuna produced too many irrelevant results for the user's target searches. `adzuna_searcher.py` deleted; `ADZUNA_APP_ID` and `ADZUNA_APP_KEY` config keys no longer needed.

## Requirements

_All requirements removed. This capability is retired._
